# <Title — imperative noun phrase, e.g. "Seed data for parallel dev databases">

**Criterion:** <# — name>
**Priority:** P0 | P1 | P2 | P3
**Effort:** S | M | L | XL
**Status:** Proposed | Todo | In progress | Done | Skipped
**First raised:** YYYY-MM-DD
**Last confirmed:** YYYY-MM-DD

## What's missing

<The concrete gap, with the evidence that proves it. "No seed command — a fresh checkout boots to an empty screen (probed: `<command>` on YYYY-MM-DD)" beats "seeding could be better".>

## Why it matters for agents

<The agent-shaped consequence — what an agent cannot do, or does slowly/fragilely, because of this gap. Tie it to a workflow: bug loop link, parallel worktrees, QA, review.>

## Implementation direction

<Concrete enough to start from: the file/module/tool to touch, the shape of the fix, any prior art in this repo. Direction, not a full spec — a full spec belongs in the plan folder if this is picked up via `/plan`.>

## Triage record

<!-- One line per triage decision; never delete old lines. -->

- YYYY-MM-DD — <Proposed | Now (→ plan folder / commit) | Todo (→ docs/todos/<slug>.md) | Skipped (<user's reason>) | Done (<what closed it>)>
