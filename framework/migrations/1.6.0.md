---
version: 1.6.0
affects: [framework, bootstrap]
---

## What changed

Adds an opt-in tool recommendation system. A new catalog at `framework/recommendations/README.md` (vendored to `.alice/recommendations/README.md`) lists third-party tools worth offering to adopters — each entry carries a detection condition (e.g. "React-based web app via `package.json`"), an honest one-or-two-sentence why-text, and project-scoped install steps. The bootstrap recipe gains a step that offers matching entries after install completes, and `/sync` gains "Step 11 — offer recommended tooling", which does the same after migration actions finish. Nothing installs without an explicit user pick; installs are always project-scoped (local dev dependency / repo-local config — never global, never user-home). Decisions persist per tool in `.alice/mem/recommendations.json` (`installed` / `declined` / `pending` / `already-present`), so settled entries are never re-offered — tools the repo carried before any offer are detected via each entry's **Detect existing** check, recorded `already-present`, and never offered or installed over.

The catalog lands via the normal Tier 1 copy; the `/sync` flow change lands via the Tier 2/3 update of `.alice/commands/sync.md`. Note that the sync bringing you to 1.6.0 runs your *current* (pre-1.6.0) command file, so the recommendations prompt first appears on your *next* `/sync`.

## Automatic actions

None. Both files arrive through the tiered file-copy; the state file is created lazily on the first offer.

## Manual actions

- [ ] Confirm `.alice/recommendations/README.md` exists after this sync (Tier 1 add).
- [ ] If you customized `.alice/commands/sync.md`, make sure the Tier 3 merge kept the new "Step 11 — offer recommended tooling" section (and the final report renumbered to Step 12).
- [ ] Optional: don't wait for the next `/sync` — ask your agent to read `.alice/recommendations/README.md` and run the offer flow now.
